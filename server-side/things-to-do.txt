program to reprompt when user choice goes wrong
make a common folder for all runtime files
proceed verification before actually running. also inform the number of hosts on. 
command runnner script from runtime files
tell the proper path for inner scripts 
line spacer for cleanliness in main program
commands for each distro to be specified in files
toggle commands to run as normal user and root user in the invoking script. to prevent non-existing file reading. pass it as a parameter to the commander script
set -e flag in commands to run as user; for both root and normal. to stop when a command fail.
user setter must call user fetcher with a parameter on who is called
also specify the file from where to execute commands to the remote host while sshing 
store the users listed by the script user to a file in user-fetcher script. to ssh to all those users

script to setup the env
packages installer script change needed at #. same for packages remover
ensure files and scripts locations are correct
perm ip for server
add the deb files location to 'files to tar' for package installation. also, other required scripts like app-opener.sh
change ./runtime-files/filename to full path as scripts are in another subdirectory and the previous location will fail. including scripts that point to deb files pointed
